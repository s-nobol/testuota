<!--トップ画-->
<%= render "layouts/top_image"%>

<!--メインバー-->
<%= render "layouts/main_bar"%>

<div class="container">
<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>

<div class="row"> 
    <!--<div class="col-2 ">-->
    <!--  <p class="border-bottom">人気の写真</p>-->
      
    <!--  <% if @user %>-->
          <!--ユーザープロフィール-->
    <!--      <%= render @user %>-->
          
          <!--ユーザーステータス--> 
    <!--      <p class="border-bottom">ユーザーリンク</p>-->
    <!--      <%= render "users/user_status" %>-->
    <!--  <% end %>-->
      
      <!--<div>-->
            <%#= render @posts %>
      <!--</div>-->
      
    <!--</div>-->
    
    <!--写真一覧ページのみ写真の表示を大きくしたい-->
    <!--なのでべつにrender入れなくてもいいかも-->
    <div class="col-12 border-bottom m-auto">
      <p  class="border-bottom"></p>
      
      <% @posts.each do |post| %>
        <picture>
          <%= link_to post, class:"text-white" do %>
          
              <!--// 画像があるときは画像を表示する-->
              <% if post.picture.thumb.present? %>
                <%= image_tag(post.picture.url(:thumb) , class:"image_form m-1", id:"image", width:"200" ,height:"130" ) %>
              <% else %>
                <%= image_tag("no_image.jpg" , class:"img-fluid m-1", id:"image", width:"260" ,height:"200") %>
              <% end %>
              
          <% end %>
        </picture>
      <% end %>
      <%= paginate  @posts, window: 2 %>
      <%#= render @posts %>
    </div>
</div>
</div>
